# This file documents possible server configuration without
# the risk of checking sensitive variables into the repository.
# Rename it to `.env` and then feel free to add sensitive values as `.env` is in the `.gitignore`.

# Server listening
HOST=0.0.0.0
PORT=8080

# Serve static with express (disable if static served with nginx for example)
EXPRESS_SERVING_STATIC=true

# For static renderer
# ROUTER_CONFIG=./src/routes

# Add babel polyfill
# BABEL_POLYFILL=true

# Babel plugin transform runtime
# BABEL_RUNTIME=true

# Add express app
# EXPRESS_APP=./api/index.js
